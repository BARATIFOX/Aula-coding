#include <stdio.h>
int main(void) {
    int n;
    int i = 1;
    int soma = 0;

    printf("Digite o valor de N: ");
    scanf("%d", &n);
    while (i <= n) {
        soma += i; 
        i++;      
    }
    printf("Soma = %d\n", soma);

    return 0;
}
